{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd website && npm install && npm run build",
  "outputDirectory": "website/build",
  "framework": null,
  "devCommand": "cd website && npm start",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/",
      "status": 200
    }
  ]
}
